# 🚨 КРИТИЧЕСКИ ВАЖНОЕ РУКОВОДСТВО ДЛЯ AI АГЕНТОВ - AGAPE WORSHIP

## 🎯 ЦЕЛЬ ДОКУМЕНТА
Дать новому AI агенту **ПОЛНОЕ** понимание проекта за 5 минут, избежав всех известных проблем.

## 🏗️ АРХИТЕКТУРА (КРИТИЧНО!)

```
ГИБРИДНАЯ СИСТЕМА = Legacy код + Новые модули
├── Legacy (НЕ ТРОГАТЬ без крайней необходимости!)
│   ├── script.js (900 строк) - главный контроллер
│   ├── ui.js (1500+ строк) - UI функции  
│   └── firebase-init.js - инициализация Firebase v8
│
├── Модульная (/src/modules/)
│   ├── auth/ - авторизация, роли, статусы, гостевой вход
│   ├── branches/ - филиалы и заявки
│   ├── admin/ - админ панель
│   ├── settings/ - настройки пользователя
│   ├── songs/ - оверлей песен и репертуара
│   ├── events/ - события/служения с участниками (NEW!)
│   └── logger.js - условное логирование
│
└── Стили (/styles/)
    ├── buttons.css, forms.css, modals.css - базовые
    ├── overlays.css - оверлеи песен/репертуара
    └── responsive.css - мобильная адаптация
```

## ⚠️ КРИТИЧЕСКИЕ ФАЙЛЫ - НЕ ПЕРЕМЕЩАТЬ!

1. **sw.js** - ДОЛЖЕН быть в корне (PWA требование) - текущая версия v346
2. **ui.js** - НЕ перемещать (сломается весь UI)
3. **script.js** - НЕ перемещать (главный контроллер)
4. **firebase-init.js** - НЕ менять (v8 через CDN)
5. **firebase-v8-adapter.js** - критический адаптер для v9 синтаксиса
6. **logger.js** - условное логирование (используй вместо console.log!)
7. **/public/event/** - страница события (не трогать пути!)

## 🔥 FIREBASE - ТОЛЬКО v8!

```javascript
// ✅ ПРАВИЛЬНО (v8):
const snapshot = await docRef.get();
if (snapshot.exists) { // НЕ exists()!
    const data = snapshot.data();
}

// ❌ НЕПРАВИЛЬНО (v9):
if (snapshot.exists()) // ОШИБКА!

// ✅ ПРАВИЛЬНЫЙ импорт logger:
import logger from '../../utils/logger.js'; // default export!
logger.log('Сообщение'); // НЕ console.log!

// ✅ ПРАВИЛЬНАЯ работа с Timestamp:
const Timestamp = window.firebase.firestore.Timestamp;
createdAt: Timestamp.now()

// ❌ НЕПРАВИЛЬНО:
import { Timestamp } from 'firebase/firestore'; // v9!
```

**ВАЖНО**: Используется Firebase v8.10.0 через CDN. НЕ МЕНЯТЬ ВЕРСИЮ!

## 👤 ПРЕДПОЧТЕНИЯ ПОЛЬЗОВАТЕЛЯ (из memories)

1. **НЕ коммитить автоматически** - спрашивать подтверждение
2. **Давать пошаговые инструкции** - что делать, как, зачем
3. **Писать детальные планы** перед большими изменениями
4. **Минимум emoji** в тексте - отвлекают
5. **Использовать ветку main** для GitHub Pages (не masters)
6. **Вежливые формулировки** в стиле подсказок
7. **Модульность и малые файлы** (<300 строк) для Cursor

## 🎯 ТЕКУЩЕЕ СОСТОЯНИЕ ПРОЕКТА

### Система ролей и статусов:
- **Роли**: `admin`, `user`, `guest` 
- **Статусы**: `active`, `pending`, `blocked`, `guest`
- **Новые пользователи**: автоматически получают `pending`
- **Гостевой вход**: анонимная авторизация без профиля в Firestore
- **Основатель**: неизменяемая роль, защищен от удаления

### Методы авторизации:
- ✅ Email/пароль
- ✅ Google
- ✅ Apple
- ✅ Анонимный (гостевой) вход
- ❌ Телефон (УДАЛЕН из-за проблем с reCAPTCHA)

### Филиалы (branches):
- Основной филиал: `branchId`
- Дополнительные: `additionalBranchIds[]`
- Селектор филиалов в панели сетлистов
- Система заявок на смену/добавление
- **ВАЖНО**: Проверка прав доступа через `canEditInCurrentBranch()`

### Ограничения для pending и guest:
- ❌ Создание сетлистов
- ❌ Удаление сетлистов
- ❌ Добавление/удаление песен
- ❌ Редактирование названий
- ❌ Подача заявок на филиалы (guest)
- ✅ Просмотр всего контента
- **ВАЖНО**: Все защищенные кнопки должны показывать alert при клике!

### UI/UX Изменения:
- **Оверлей песен**: Красивый выбор с фильтрами по категориям
- **Оверлей репертуара**: User-specific репертуар с тональностями
- **Фон**: #111827 (темная тема), #d4d8de (светлая тема)
- **Шрифт**: Montserrat везде, КРОМЕ текста песен (моноширинный)
- **PWA**: Service Worker v243
- **Темы**: темная по умолчанию, улучшенная светлая
- **Логирование**: условное через logger.js
- **CSS переменные**: var(--primary-color), var(--bg-secondary), etc.
- **Стили и дизайн**: См. `/documentation/STYLE_GUIDE.md` для детального руководства

### Новые функции:
1. **Оверлей "Все песни"** (`/src/modules/songs/songsOverlay.js`)
   - Двухуровневые фильтры: основные + подкатегории
   - Отображение тональности и BPM
   - Поиск и фильтрация

2. **Репертуар пользователя** (`/src/modules/songs/repertoireOverlay.js`)
   - Структура: `users/[uid]/repertoire/[songId]`
   - Одна тональность на песню (preferredKey)
   - Фильтры по категориям и тональностям
   - Кнопка удаления в карточке песни

3. **События/Служения** (`/src/modules/events/`)
   - Создание событий с привязкой к сетлистам
   - Управление участниками и инструментами
   - Отдельная страница события с плеером
   - Публичные ссылки для шаринга
   - **Улучшения UI январь 2025**:
     - Убраны линии сетки календаря
     - Исправлены селекторы (проблема с обрезанием текста)
     - Добавлена кнопка выбора ведущего из модального окна
     - Оптимизированы отступы между полями формы
     - Минимизирован дизайн модального окна участников
     - Исправлена проблема с клавиатурой на мобильных
     - Устранены пробелы в dropdown через DOM манипуляцию
     - **Новый дизайн карточек событий в режиме списка**:
       - Улучшенная структура шапки (название слева, дата/время справа)
       - Интерактивный разворачивающийся список участников
       - Визуальные эффекты (тени, hover, анимации)
       - Адаптивный дизайн для мобильных
     - **Исправленные баги**:
       - Правильная передача даты из календаря (локальное время)
       - Создатель не добавляется автоматически в участники
       - Корректное отображение количества песен в календаре
     - **Модернизация страницы события**:
       - Поддержка светлой/темной тем (убрана принудительная темная)
       - Современный дизайн с backdrop-filter
       - Разворачивающийся список участников
       - Единый порядок отображения инструментов
     - **Улучшения плеера событий**:
       - Новая 2-строчная структура шапки
       - Кнопка скрытия панели инструментов
       - Увеличен базовый размер шрифта песен (12px)
       - Тональность в квадратных метках
   - ВАЖНО: См. `/documentation/EVENTS_FEATURE_GUIDE.md`

4. **Защита от мигания UI**
   - Атрибут `data-song-loaded` на body
   - CSS скрывает элементы до загрузки песни

## 🐛 ИЗВЕСТНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### 1. "Cannot find module"
**Причина**: Неправильный путь после реорганизации
**Решение**: Проверить actual path, использовать ../

### 2. "doc.exists is not a function"
**Причина**: v9 синтаксис с v8 Firebase
**Решение**: `.exists` вместо `.exists()`

### 3. "404 на продакшене"
**Причина**: Не обновлен Service Worker
**Решение**: Увеличить версию в sw.js и CACHE_NAME

### 4. Дублирование импортов
**Причина**: Переходный период Legacy → Модули
**Решение**: Это нормально, не трогать

### 5. UI мигание при загрузке (РЕШЕНО!)
**Причина**: Элементы управления песнями видны до выбора песни
**Решение**: CSS с data-song-loaded атрибутом

### 6. Репертуар виден всем пользователям
**Причина**: Добавление в `users/[songId]` вместо `users/[uid]/repertoire/[songId]`
**Решение**: ВСЕГДА использовать подколлекцию repertoire!

### 7. Кнопки не видны в темах
**Причина**: Неправильные цвета или отсутствие !important
**Решение**: Проверить CSS переменные и контраст (см. STYLE_GUIDE.md)

### 8. Branch permissions не работают
**Причина**: Визуальное отключение без функционального
**Решение**: Добавить обработчики с alert для защищенных действий

### 9. Проблема с первой регистрацией
**Причина**: Race condition при выборе филиала
**Решение**: Дополнительные проверки в branchSelector.js

### 10. Пробелы в dropdown при подсветке поиска
**Причина**: HTML генерация с переносами строк
**Решение**: Использовать DOM манипуляцию вместо innerHTML или генерировать HTML в одну строку

## ❌ ЧЕГО НЕ ДЕЛАТЬ

1. **НЕ менять версию Firebase** (сломается всё)
2. **НЕ перемещать корневые файлы** (ui.js, script.js)
3. **НЕ удалять HTML-редиректы** в корне
4. **НЕ трогать legacy код** без крайней необходимости
5. **НЕ создавать ветку masters** (только main)
6. **НЕ менять моноширинный шрифт** для песен
7. **НЕ делать файлы больше 300 строк** (Cursor limitation)
8. **НЕ использовать console.log** напрямую (только logger.log)
9. **НЕ добавлять данные в корень users** (используй подколлекции)

## 🚀 РЕКОМЕНДАЦИИ

### При изменениях:
1. **ВСЕГДА** обновляй Service Worker версию
2. Проверяй все импорты (особенно logger.js)
3. Используй параллельные tool calls для эффективности
4. Используй logger.js вместо console.log
5. **НЕ коммить автоматически** - спрашивай пользователя
6. Тестируй в обеих темах (темная/светлая)

### Новый функционал:
- Создавай в `/src/modules/`
- Используй firebase-v8-adapter
- Следуй модульной архитектуре
- Файлы < 300 строк
- Добавляй стили в отдельные файлы в `/styles/`
- Используй CSS переменные для цветов

### При ошибках:
1. Проверь версию Firebase (должна быть v8)
2. Проверь пути импортов (особенно logger)
3. Проверь `.exists` vs `.exists()`
4. Очисти кэш (Service Worker)
5. Проверь права доступа пользователя
6. Проверь обе темы

### CSS и стилизация:
1. Используй CSS переменные из variables.css
2. Проверяй контраст в обеих темах
3. Мобильные стили в responsive.css
4. Для оверлеев используй overlays.css
5. **ВАЖНО**: !important для критичных цветов
6. **НОВОЕ**: См. `/documentation/STYLE_GUIDE.md` для полного руководства по стилям

## 📊 СТРУКТУРА БД FIRESTORE

```
users/
├── uid
├── email
├── name
├── role: admin|user|guest
├── status: active|pending|blocked
├── branchId: основной филиал
├── additionalBranchIds: []
├── createdAt, approvedAt, etc.
└── repertoire/ (подколлекция!)
    └── [songId]
        ├── name
        ├── preferredKey (одна тональность!)
        ├── BPM
        ├── category
        └── addedAt
(Гостевые пользователи НЕ создаются в БД!)

branches/
├── name
├── address
├── createdBy
└── createdAt

worship_setlists/
├── name
├── songs: [{songId, order, preferredKey}]
├── branchId
├── createdBy
└── createdAt

songs/
├── name (название песни = ID документа!)
├── text / Текст и аккорды
├── defaultKey / Оригинальная тональность
├── sheet (категория)
├── BPM / bpm
└── youtubeLink

events/ (NEW!)
├── name
├── date: Timestamp
├── setlistId
├── leaderId, leaderName (денормализовано)
├── songCount (денормализовано)
├── participants: [{instrumentId, userId, userName}]
├── participantCount (денормализовано)
├── comment
├── branchId
├── createdBy, createdAt, updatedAt
└── isArchived: false

branchRequests/
├── userId
├── requestedBranchId
├── type: 'change' | 'add'
├── status: 'pending' | 'approved' | 'rejected'
└── createdAt
```

## 🔍 ПОЛЕЗНЫЕ КОМАНДЫ

```bash
# Найти использование функции
grep -r "functionName" --include="*.js"

# Проверить импорты файла
grep -r "from.*filename" --include="*.js"

# Найти все TODO
grep -r "TODO" --include="*.js"

# Найти все console.log (должны быть заменены на logger.log)
grep -r "console\.log" --include="*.js"

# Проверить размер файлов (не должны быть > 300 строк)
find . -name "*.js" -exec wc -l {} + | sort -n
```

## 🎨 ВАЖНЫЕ UI/UX ПАТТЕРНЫ

1. **Оверлеи**: Используй готовый паттерн из songsOverlay.js
2. **Защищенные действия**: Визуальное отключение + alert
3. **Фильтры**: Двухуровневая система (основной + подфильтры)
4. **Карточки песен**: Единый стиль для списков
5. **Кнопки удаления**: Всегда красные, видны при hover
6. **Состояния загрузки**: Используй data-attributes

## 💡 ГЛАВНЫЙ СОВЕТ

**Начни с чтения этого файла, затем AI_AGENT_GUIDE.md для деталей.**
Если сомневаешься - спроси пользователя, не делай предположений.
Помни о предпочтениях пользователя и НЕ нарушай критические правила.
При работе с репертуаром ВСЕГДА проверяй путь в Firestore!

---
**Последнее обновление**: Январь 2025
**Критичность**: МАКСИМАЛЬНАЯ
**Service Worker**: v346
**Новое**: События/служения - см. EVENTS_FEATURE_GUIDE.md, Стили - см. STYLE_GUIDE.md