# 📚 ДЕТАЛЬНОЕ РУКОВОДСТВО ДЛЯ AI АГЕНТОВ - AGAPE WORSHIP

## 🎯 О ПРОЕКТЕ
**Agape Worship** - веб-приложение для управления песнями прославления с поддержкой филиалов, ролей и офлайн режима.

- **URL**: https://agapeworship.asia/
- **GitHub**: https://github.com/exserega/worshiptest
- **Архитектура**: Гибридная (Legacy + Модульная)
- **Firebase**: v8.10.0 (CDN)
- **UI Framework**: Vanilla JS + CSS
- **Шрифт**: Montserrat (кроме текста песен)

## 📁 СТРУКТУРА ПРОЕКТА

### Корневые файлы (КРИТИЧНО - НЕ ПЕРЕМЕЩАТЬ!)
```
/ (корень)
├── index.html          # Главная страница
├── script.js           # Главный контроллер (~900 строк)
├── ui.js               # UI функции (~1500 строк)
├── firebase-init.js    # Firebase v8 инициализация
├── styles.css          # Основные стили (5000+ строк)
├── sw.js               # Service Worker для PWA (v109)
├── manifest.json       # PWA манифест
└── CNAME               # GitHub Pages домен
```

### Модульная архитектура
```
/src/
├── modules/           # Новый функционал
│   ├── auth/         # Авторизация, роли, гостевой вход
│   ├── branches/     # Управление филиалами
│   ├── admin/        # Админ панель
│   ├── settings/     # Настройки пользователя
│   └── songs/        # Оверлей выбора песен (NEW!)
├── api/              # API функции
├── utils/            
│   ├── firebase-v8-adapter.js  # Адаптер v9→v8 (КРИТИЧНО!)
│   └── logger.js              # Условное логирование (NEW!)
└── main/             # Инициализация
```

### Другие директории
```
/public/              # HTML страницы
├── admin.html       # Админ панель
├── login.html       # Авторизация
└── settings.html    # Настройки

/css/                # Стили страниц
/assets/             # Изображения, звуки
/js/                 # Переэкспортированные модули
/styles/             # Модульные стили (NEW!)
├── overlays.css     # Стили оверлеев
├── controls.css     # Стили элементов управления
└── ...              # Другие модульные стили
```

## 🔥 FIREBASE АДАПТЕР

**КРИТИЧНО**: Используется адаптер для v9-подобного синтаксиса с v8 API:

```javascript
// Импорт через адаптер
import { db, collection, addDoc } from 'src/utils/firebase-v8-adapter.js';

// v8 синтаксис для документов
if (snapshot.exists) { // НЕ exists()!
    const data = snapshot.data();
}
```

## 🔐 СИСТЕМА АВТОРИЗАЦИИ

### Методы входа
- **Email/пароль** - стандартная регистрация
- **Google** - OAuth авторизация
- **Apple** - Sign in with Apple (NEW!)
- **Анонимный** - гостевой вход без регистрации (NEW!)
- ~~**Телефон**~~ - УДАЛЕН из-за проблем с reCAPTCHA

### Роли пользователей
- **admin** - полный доступ, управление пользователями
- **user** - стандартный пользователь
- **guest** - ограниченный доступ (анонимные пользователи)

### Статусы пользователей  
- **active** - полный доступ к функционалу
- **pending** - ожидает одобрения (ограничения)
- **blocked** - заблокирован
- **guest** - анонимный пользователь (NEW!)

### Гостевые пользователи (NEW!)
- Вход через анонимную авторизацию Firebase
- НЕ создаются в Firestore
- Хранятся только в памяти приложения
- Ограничения как у pending + нет профиля
- При выходе все данные теряются

### Основатель
- Email в коллекции `appConfig/founder`
- Неизменяемая роль admin
- Защита от удаления

## 🏢 СИСТЕМА ФИЛИАЛОВ

### Структура
- **branchId** - основной филиал пользователя
- **additionalBranchIds[]** - дополнительные филиалы
- Селектор в панели сетлистов
- Ограничения редактирования вне своих филиалов

### Заявки
- На вступление в филиал (новые пользователи)
- На смену основного филиала
- На добавление дополнительного филиала
- Управление через админ панель
- Гостевые пользователи НЕ могут подавать заявки

## 🎵 ФУНКЦИОНАЛ ПЕСЕН

### Основные возможности
- ~~Категории песен (select)~~ - УДАЛЕНО
- **Оверлей выбора песен** (NEW!) - полноэкранный список
- Двухуровневая фильтрация категорий
- Транспонирование тональности
- Подсветка аккордов
- YouTube ссылки
- Режим презентации
- Поиск с подсветкой результатов

### Оверлей песен (NEW!)
- Кнопка "Все песни" открывает полноэкранный список
- Фильтры: Все | Быстрые | Поклонение
- Подфильтры: Вертикаль | Горизонталь
- Отображение тональности и BPM
- Клик по песне открывает её на главном экране

### Сетлисты
- Привязаны к филиалам
- Создание/редактирование (для active)
- Ограничения для pending и guest пользователей
- Drag & drop сортировка
- Выбор песен из оверлея

## 🎨 UI/UX

### Темы
- Темная тема по умолчанию (#111827)
- Светлая тема
- Сохранение в localStorage
- CSS переменные для всех цветов

### PWA
- Офлайн режим
- Установка на устройство
- Service Worker кэширование (v109)
- manifest.json с правильными путями

### Адаптивность
- Mobile-first подход
- Боковые панели
- Модальные окна
- Оверлеи для мобильных

### UI Улучшения (NEW!)
- Устранено мигание при загрузке (data-song-loaded)
- Улучшена навигация в мобильной версии
- Кнопки с эффектом объема
- Единый дизайн системы

## ⚡ ОПТИМИЗАЦИЯ

### Условное логирование (NEW!)
```javascript
import logger from './src/utils/logger.js';
logger.log('Сообщение'); // Только в dev режиме
```

### Web Workers
- Поиск песен в отдельном потоке
- Обновление базы при загрузке

### Lazy Loading
- Динамическая загрузка модулей
- Загрузка изображений по требованию

### Кэширование
- Service Worker для офлайн
- localStorage для настроек
- Версионирование кэша (v109)

## 🛠️ РАЗРАБОТКА

### Правила разработки
1. **Файлы < 300 строк** - ограничение Cursor
2. **Модульность** - разделять большие файлы
3. **Использовать logger.js** вместо console.log
4. **НЕ коммитить автоматически** - спрашивать пользователя

### Добавление функционала
1. Создавать в `/src/modules/`
2. Использовать ES6 модули
3. Следовать существующим паттернам
4. Обновлять Service Worker
5. Добавлять стили в `/styles/`

### Тестирование
```bash
# Локальный сервер
python3 -m http.server 8000

# Очистка кэша
DevTools → Application → Clear Storage
```

### Деплой
- Автоматический через GitHub Pages
- Ветка main
- Домен через CNAME
- Обновление через git push

## 📝 СОГЛАШЕНИЯ

### Код
- ES6+ синтаксис
- async/await для промисов
- Комментарии на русском
- JSDoc для функций
- Использовать logger.js

### Стили
- CSS переменные для тем
- BEM-подобная нотация
- Mobile-first медиа запросы
- Модульные CSS файлы в /styles/

### Коммиты
- Conventional Commits
- feat/fix/docs/style/refactor
- Описание на английском
- Детальные сообщения коммитов

---
**См. также**: AI_AGENT_CRITICAL_GUIDE.md для критически важной информации
**Последнее обновление**: Август 2025