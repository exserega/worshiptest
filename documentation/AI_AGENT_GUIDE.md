# 📚 ДЕТАЛЬНОЕ РУКОВОДСТВО ДЛЯ AI АГЕНТОВ - AGAPE WORSHIP

## ⚠️ КРИТИЧЕСКИ ВАЖНО: ТОЛЬКО ТЕМНАЯ ТЕМА!
**Весь проект работает ИСКЛЮЧИТЕЛЬНО с темной темой!**
- НЕ разрабатывайте стили для светлой темы
- НЕ тестируйте светлую тему
- Темная тема - единственная активная тема

## 🎯 О ПРОЕКТЕ
**Agape Worship** - веб-приложение для управления песнями прославления с поддержкой филиалов, ролей и офлайн режима.

- **URL**: https://agapeworship.asia/
- **GitHub**: https://github.com/exserega/worshiptest
- **Архитектура**: Гибридная (Legacy + Модульная)
- **Firebase**: v8.10.0 (CDN)
- **UI Framework**: Vanilla JS + CSS
- **Шрифт**: Montserrat везде, КРОМЕ текста песен (моноширинный)
– **PWA**: Service Worker (см. README текущую версию)
– **Темы**: ТОЛЬКО тёмная (светлая — legacy)

## 📁 СТРУКТУРА ПРОЕКТА

### Корневые файлы (КРИТИЧНО - НЕ ПЕРЕМЕЩАТЬ!)
```
/ (корень)
├── index.html          # Главная страница
├── script.js           # Главный контроллер (~900 строк)
├── ui.js               # UI функции (~1500 строк)
├── firebase-init.js    # Firebase v8 инициализация
├── styles.css          # Основные стили (5000+ строк)
├── sw.js               # Service Worker для PWA (v630k)
├── manifest.json       # PWA манифест
└── CNAME               # GitHub Pages домен
```

### Модульная архитектура
```
/src/
├── modules/           # Новый функционал
│   ├── auth/         # Авторизация, роли, гостевой вход
│   ├── branches/     # Управление филиалами
│   ├── admin/        # Админ панель
│   ├── settings/     # Настройки пользователя
│   ├── songs/        # Оверлеи песен и репертуара
│   │   ├── songsOverlay.js      # Оверлей "Все песни"
│   │   └── repertoireOverlay.js # Оверлей "Репертуар"
│   ├── archive/      # Модули архива сет-листов
│   │   ├── archiveApi.js         # CRUD операции архива
│   │   ├── archiveGroupsApi.js   # API для групп
│   │   ├── archiveGroupsManager.js # UI управления группами
│   │   ├── archiveSongsOverlay.js # Создание сет-листов
│   │   └── archiveSaveModal.js    # Модалка сохранения в архив
│   └── events/       # Модули событий/календаря
│       ├── eventPlayer.js        # Просмотр сет-листов
│       ├── eventsApi.js          # API событий
│       └── eventCreationModal.js # Создание событий
├── api/              # API функции
├── utils/            
│   ├── firebase-v8-adapter.js  # Адаптер v9→v8 (КРИТИЧНО!)
│   ├── logger.js              # Условное логирование
│   └── setlistUtils.js        # Универсальный доступ к сет-листам
└── main/             # Инициализация
```

### Другие директории
```
/public/              # HTML страницы
├── admin.html       # Админ панель
├── login.html       # Авторизация
├── settings.html    # Настройки
└── event/           # Страница события

/archive/            # Архив сет-листов
├── index.html       # Страница архива
├── archive.js       # Основная логика
├── *.css            # Стили архива

/css/                # Стили страниц
/assets/             # Изображения, звуки
/js/                 # Переэкспортированные модули
/styles/             # Модульные стили
├── overlays.css     # Стили оверлеев
├── controls.css     # Стили элементов управления
├── buttons.css      # Стили кнопок
├── forms.css        # Стили форм
├── modals.css       # Стили модальных окон
├── panels.css       # Стили панелей
├── responsive.css   # Мобильная адаптация
├── variables.css    # CSS переменные
└── archive-save-modal.css # Модалка архива
```

## 🔥 FIREBASE

**КРИТИЧНО**: Используется ТОЛЬКО v8 SDK (CDN).

```javascript
// v8 синтаксис для документов
if (snapshot.exists) { // НЕ exists()!
    const data = snapshot.data();
}

// Правильный импорт logger
import { logger } from '../logger.js'; // Путь зависит от файла
logger.log('Message'); // НЕ console.log!
```

## 🔐 СИСТЕМА АВТОРИЗАЦИИ

### Методы входа
- **Email/пароль** - стандартная регистрация
- **Google** - OAuth авторизация
- **Apple** - Sign in with Apple
- **Анонимный** - гостевой вход без регистрации
- ~~**Телефон**~~ - УДАЛЕН из-за проблем с reCAPTCHA

### Роли пользователей
- **admin** - полный доступ, управление пользователями
- **user** - стандартный пользователь
- **guest** - ограниченный доступ (анонимные пользователи)

### Статусы пользователей  
- **active** - полный доступ к функционалу
- **pending** - ожидает одобрения (ограничения)
- **blocked** - заблокирован
- **guest** - анонимный пользователь

### Гостевые пользователи
- Вход через анонимную авторизацию Firebase
- НЕ создаются в Firestore
- Хранятся только в памяти приложения
- Ограничения как у pending + нет профиля
- При выходе все данные теряются
- Специальное сообщение вместо "заявка на рассмотрении"

### Основатель
- Email в коллекции `appConfig/founder`
- Неизменяемая роль admin
- Защита от удаления

## 🏢 СИСТЕМА ФИЛИАЛОВ

### Структура
- **branchId** - основной филиал пользователя
- **additionalBranchIds[]** - дополнительные филиалы
- Селектор в панели сетлистов
- Ограничения редактирования вне своих филиалов

### Заявки
- На вступление в филиал (новые пользователи)
- На смену основного филиала
- На добавление дополнительного филиала
- Управление через админ панель
- Гостевые пользователи НЕ могут подавать заявки

### Проверка прав
```javascript
import { canEditInCurrentBranch } from '../branches/branchSelector.js';
const canEdit = await canEditInCurrentBranch();
if (!canEdit) {
    alert('Недоступно. Вы не состоите в этом филиале');
    return;
}
```

## 🎵 ФУНКЦИОНАЛ ПЕСЕН

### Основные возможности
- **Оверлей выбора песен** - полноэкранный список
- **Репертуар пользователя** - личные тональности
- Двухуровневая фильтрация категорий
- Транспонирование тональности
- Подсветка аккордов
- YouTube ссылки
- Режим презентации
- Поиск с подсветкой результатов
- Копирование текста песни (ВИДИМОГО контента на главной)

### Оверлей песен
- Кнопка "Все песни" открывает полноэкранный список
- Фильтры: Все | Быстрые | Поклонение
- Подфильтры: Вертикаль | Горизонталь
- Отображение тональности и BPM
- Клик по песне открывает её на главном экране
- Красивая анимация открытия/закрытия

### Репертуар пользователя
- Кнопка "Репертуар" в мобильном меню
- Хранится в `users/[uid]/repertoire/[songId]`
- Одна тональность на песню (preferredKey)
- Фильтры: Все | Быстрые | Поклонение | Тональность
- Кнопка микрофона для добавления в репертуар
- Красная кнопка удаления в карточке песни
- При добавлении в другой тональности - запрос на замену

### Сетлисты
- Привязаны к филиалам
- Создание/редактирование (для active)
- Ограничения для pending и guest пользователей
- Drag & drop сортировка
- Выбор песен из оверлея
- Кнопки защищены alert-сообщениями
  
## ✏️ Overrides (гибридная система правок)
- Хранение:
  - Base (Sheets) — `songs/{songId}` (строка в массиве), неизменяемо клиентом
  - User — `users/{uid}/user_overrides/{songId}`
  - Global (branch) — `songs/{songId}/overrides/global_{branchId}`
  - History — `songs/{songId}/override_history`
- Приоритет отображения: `user → global → base`
- Подписка: `subscribeResolvedContent(songId, onChange, onGlobalUpdateForUser, { viewerBranchId })`
- Интеграции: главная (displaySongDetails), редактор (openSongEditor), мобильный оверлей, Event Player
- Гости в Event видят глобальные правки филиала

## 🎨 UI/UX

### Темы
- Темная тема по умолчанию (#111827)
- Светлая тема — legacy (не поддерживается)
- CSS переменные для всех цветов
- Высокая контрастность

### CSS Переменные
```css
/* Основные цвета */
--primary-color: #22d3ee; /* темная тема */
--bg-secondary: #1f2937;
--text-primary: #e5e7eb;
--text-secondary: #9ca3af;
/* И другие в variables.css */
```

### PWA
- Офлайн режим
- Установка на устройство
- Service Worker кэширование (v630k)
- manifest.json с правильными путями
- Обновление кэша при bump версии

### Адаптивность
- Mobile-first подход
- Боковые панели
- Модальные окна
- Оверлеи для мобильных
- Кнопки адаптируются под размер экрана

### UI Улучшения (актуальные)
- Устранено мигание при загрузке (data-song-loaded)
- Единый Clear-X в поиске (32x32, скрытие лупы через :has)
- Главный поиск: приоритет названий над текстом
- Копирование только видимого текста песни
- Убраны иконки у заголовков блоков на главной

## ⚡ ОПТИМИЗАЦИЯ

### Условное логирование
```javascript
import { logger } from './src/utils/logger.js';
logger.log('Сообщение'); // Только в dev режиме
logger.error('Ошибка'); // Важные ошибки
```

### Web Workers
- Поиск песен в отдельном потоке
- Обновление базы при загрузке

### Lazy Loading
- Динамическая загрузка модулей
- Загрузка изображений по треб demand
- Оверлеи загружаются по требованию

### Кэширование
- Service Worker для офлайн
- localStorage для настроек
- Версионирование кэша (v630k)
- Стратегия network-first

## 🛠️ РАЗРАБОТКА

### Правила разработки
1. **Файлы < 300 строк** - ограничение Cursor
2. **Модульность** - разделять большие файлы
3. **Использовать logger.js** вместо console.log
4. **НЕ коммитить автоматически** - спрашивать пользователя
5. **Тестировать на мобильных (400-460px ширина)**
6. **Проверять права доступа**

### Добавление функционала
1. Создавать в `/src/modules/`
2. Использовать ES6 модули
3. Следовать существующим паттернам
4. Обновлять Service Worker версию
5. Добавлять стили в `/styles/`
6. Использовать CSS переменные

### Типичные паттерны

#### Защищенная кнопка:
```javascript
button.addEventListener('click', async () => {
    if (userStatus === 'pending' || userStatus === 'guest') {
        alert('Недоступно. Ваша заявка на рассмотрении');
        return;
    }
    // Основная логика
});
```

#### Проверка филиала:
```javascript
const canEdit = await canEditInCurrentBranch();
if (!canEdit) {
    element.style.opacity = '0.5';
    element.style.cursor = 'not-allowed';
    element.classList.add('branch-disabled');
}
```

#### Работа с репертуаром:
```javascript
// ПРАВИЛЬНО
const repertoireRef = db.collection('users').doc(userId).collection('repertoire');

// НЕПРАВИЛЬНО
const wrongRef = db.collection('users').doc(songId); // НЕТ!
```

### Тестирование
```bash
# Локальный сервер
python3 -m http.server 8000

# Очистка кэша
DevTools → Application → Clear Storage

# Проверка Service Worker
DevTools → Application → Service Workers
```

### Деплой
- Автоматический через GitHub Pages
- Ветка main
- Домен через CNAME
- Обновление через git push
- Проверка: https://agapeworship.asia/

## 📝 СОГЛАШЕНИЯ

### Код
- ES6+ синтаксис
- async/await для промисов
- Комментарии на русском
- JSDoc для функций
- Использовать logger.js
- Деструктуризация для чистоты

### Стили
- CSS переменные для тем
- BEM-подобная нотация
- Mobile-first медиа запросы
- Модульные CSS файлы в /styles/
- !important только где критично
- Группировка по компонентам

### Коммиты
- Conventional Commits
- feat/fix/docs/style/refactor
- Описание на английском
- Детальные сообщения коммитов
- Версия SW в сообщении

## 🔍 ПОЛЕЗНЫЕ ПРИМЕРЫ

### Создание оверлея:
```javascript
const overlay = document.createElement('div');
overlay.className = 'songs-overlay';
overlay.innerHTML = `
    <div class="songs-overlay-content">
        <div class="overlay-header">
            <h2>Заголовок</h2>
            <button class="close-btn">×</button>
        </div>
        <!-- Контент -->
    </div>
`;
```

### Работа с Firebase v8:
```javascript
// Получение документа
const doc = await db.collection('users').doc(userId).get();
if (doc.exists) { // НЕ exists()!
    const data = doc.data();
}

// Подписка на изменения
const unsubscribe = db.collection('songs')
    .onSnapshot(snapshot => {
        snapshot.forEach(doc => {
            logger.log(doc.id, doc.data());
        });
    });
```

---
**См. также**: AI_AGENT_CRITICAL_GUIDE.md для критически важной информации
**Последнее обновление**: Январь 2025
**Service Worker**: v630k